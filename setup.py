from setuptools import setup, find_packages

setup(
    name="bioverge-os-sdk",
    version="0.1.0",
    description="SDK for BioVerge OS simulation and NFT platform",
    author="BioVerge OS",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
)
